# identities.local.md — git identities of this instance

<!-- Gitignored, never loaded into a session: read by path only by
/setup, /new-project and /import-project when a repository's local
commit identity is proposed (POS.0950). One line per host. -->

- <host>: <Name> <<address>>
- <company host>: <Name> <<address>>

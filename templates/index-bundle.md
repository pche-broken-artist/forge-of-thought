---
bundle: <slug>
project: <project-slug>
date: YYYY-MM-DD
origin: <where the whole came from — a site, a sender, an export>
---

# Index — bundle `<slug>`

<!-- Catalogue of one bundle (sources/<slug>/), ONE source with one
ledger entry; the rules are CLAUDE.md's (Document chain 5). This
skeleton is the one owner of the bundle index (POS.1070): the header
and the opening paragraph are its own, the per-file entry is carried
verbatim from templates/index.md (sources entry); /ingest creates the
file from it at registration if the bundle arrives without one, and
validates a supplied one against the actual contents. Per-file origin
(URL for downloaded pages, author for own notes) and dates are best
effort, never asked for. -->

<One short paragraph: what the whole bundle is and why it entered
sources.>

### `<file>`
- **What:** <what the resource is, in one or two sentences>
- **Origin:** <where it came from — author, URL, meeting, …; date best effort>
- **Role:** <free text: a standard to verify against, inspiration, a counter-example, a meeting record, …>
- **Use for:** <what to reach for it for — the questions it answers best>

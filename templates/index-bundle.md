---
bundle: <slug>
project: <project-slug>
date: YYYY-MM-DD
origin: <where the whole came from — a site, a sender, an export>
---

# Index — bundle `<slug>`

<!-- Catalogue of one bundle (sources/<slug>/): a set of related files
that counts as ONE source with one ledger entry. This skeleton is the
one owner of the bundle index's shape (POS.1070); /ingest creates the
file from it at registration if the bundle arrives without one, and
validates a supplied one against the actual contents. Purely
descriptive and, unlike the bundle's files, a rewritten catalogue —
it tracks nothing. The top-level sources/00-INDEX.md carries one entry
for the whole bundle pointing here — two levels, never deeper.
Per-file origin (URL for downloaded pages, author for own notes) and
dates are best effort, never asked for. -->

<One short paragraph: what the whole bundle is and why it entered
sources.>

| File | What it is | Origin | Date |
|---|---|---|---|
| `<file>` | <one line> | <URL or author> | <YYYY-MM-DD or —> |
